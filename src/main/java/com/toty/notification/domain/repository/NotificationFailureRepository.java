package com.toty.notification.domain.repository;

import com.toty.notification.domain.model.NotificationFailure;
import org.springframework.data.repository.CrudRepository;

public interface NotificationFailureRepository extends CrudRepository<NotificationFailure, Long> {
}

